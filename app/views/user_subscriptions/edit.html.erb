<%= render DS::Dialog.new do |dialog| %>
  <% dialog.with_header(title: "编辑订阅 \"#{@user_subscription.name}\"") %>
  <% dialog.with_body do %>
    <%= render "form", user_subscription: @user_subscription %>
  <% end %>
<% end %>
