 <%= render DS::Button.new(
        text: "退出登录",
        icon: "log-out",
        icon_position: :right,
        variant: "ghost",
        href: session_path(Current.session),
        method: :delete
      ) %>
