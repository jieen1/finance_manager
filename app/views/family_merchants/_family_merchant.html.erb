<%# locals: (family_merchant:) %>

<div class="flex justify-between items-center p-4 bg-container hover:bg-surface-hover transition-colors">
  <div class="flex w-full items-center gap-2.5">
    <% if family_merchant.logo_url %>
      <div class="w-8 h-8 rounded-full flex justify-center items-center">
        <%= image_tag family_merchant.logo_url, class: "w-8 h-8 rounded-full" %>
      </div>
    <% else %>
      <%= render partial: "shared/color_avatar", locals: { name: family_merchant.name, color: family_merchant.color } %>
    <% end %>

    <p class="text-primary text-sm truncate">
      <%= family_merchant.name %>
    </p>
  </div>
  <div class="justify-self-end">
    <%= render DS::Menu.new do |menu| %>
      <% menu.with_item(variant: "link", text: "编辑", href: edit_family_merchant_path(family_merchant), icon: "pencil", data: { turbo_frame: "modal" }) %>
      <% menu.with_item(
        variant: "button",
        text: "删除",
        href: family_merchant_path(family_merchant),
        icon: "trash-2",
        method: :delete,
        confirm: CustomConfirm.new(title: "删除商家？", body: "确定要删除 #{family_merchant.name} 吗？此操作不可逆。", btn_text: "删除商家", destructive: true)) %>
    <% end %>
  </div>
</div>
