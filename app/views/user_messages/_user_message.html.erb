<%# locals: (user_message:) %>

<div id="<%= dom_id(user_message) %>" class="px-3 py-2 rounded-2xl max-w-[85%] w-fit ml-auto mb-4" style="background:var(--aurora-em-dim);border-bottom-right-radius:4px">
  <div class="prose prose--ai-chat"><%= markdown(user_message.content) %></div>
</div>
